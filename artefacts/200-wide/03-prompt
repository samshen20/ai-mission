find three adjacent products that solve the same job for your user.
Run research mode on each; build the comparison table — one row per product, capturing "how they solve it" in two sentences (approach), plus its strength and weakness.
Add the "us" row, then identify where all three competitors are weak or identical — that gap is your opening.
Name the differentiator your feature must own in the final column: a verb + a specific dimension (e.g., "tell the shopper whether it's really on the shelf, with a confidence the others don't show").
Pick the single strongest AI feature a competitor ships and fold it into your feature or recommendations (for Meridian, the availability assistant is already that AI feature — sharpen it against what you found). Save to 03-competitors.md.
