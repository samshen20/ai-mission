consider cost line items (implementation, run cost, change management, model inference, monitoring).
consider value line items (revenue, cost saved, risk avoided, time saved).
Propose pessimistic / base / optimistic ranges for each item, with a named source benchmark per number." 
Build the three-scenario table in Sheets.
Compute payback period (months until cumulative value > cumulative cost) for each scenario.
Identify the top-2 sensitivity drivers (move ±20% and watch which moves the answer most).
write to 05-roi.xlsx