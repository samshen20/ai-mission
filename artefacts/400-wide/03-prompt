Paste your L2 (or fallback) into AI. Prompt: "Generate a Mermaid sequenceDiagram for the operation 'Store Associate looks up a customer's online cart at the POS by scanning the customer's loyalty QR code'. Participants: POS Client, Apollo Gateway, Identity Service, Cart Service, Inventory Cache, SAP ECC. Annotate each step with p95 latency. Include the failure path where the inventory cache returns no stock for one item."
Render. Verify: (1) loyalty-QR lookup → Identity Service; (2) cart pull → Cart Service; (3) per-line-item availability check → Inventory Cache (not SAP directly — SAP is the source-of-truth that hydrates the cache async via Kafka).
Add an alt block for the failure path: cache miss on one SKU → query SAP ECC inline (slow, +400ms). Annotate. Total it up.
Now prompt: "Generate a Mermaid dependency graph (graph TD) showing which containers the POS Client depends on transitively for this operation. Include synchronous dependencies (solid arrows) and async dependencies (dashed arrows for Kafka)." Save as 03-deps.mmd.
Specify the contract for the single most critical integration in this flow — the POS Client → Apollo Gateway cart-lookup call. Prompt: "Write the integration contract for this call: API style (REST/GraphQL/gRPC), auth method, request and response payload shape, and the error-code mapping for at least three failure cases (unknown loyalty ID, cart not found, inventory-cache miss)." Save as 03-integrations.md. Force a named resource, verb, auth header, and an explicit error-code-to-user-state mapping — reject "returns an error."
Sum the latency on the happy path. Compare against your PSD2 SCA latency budget (or use 1500ms as a working target for in-store cart hydration). Note the worst stage — almost always the SAP inline fallback. Write one sentence at the bottom of 03-flow-instore-cart.mmd: "Bottleneck: SAP ECC inline lookup at ~400ms p95 dominates when the inventory cache misses. Primary mitigation lever: cache warm-up on Kafka stock events; secondary: degrade to 'stock unknown — confirm with floor staff' on cache miss."
